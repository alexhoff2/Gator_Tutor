<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>GatorTutor - <%= tutor.username %>'s Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/@yaireo/tagify/dist/tagify.css" rel="stylesheet" type="text/css" />
    <link rel="stylesheet" href="/css/tailwind.css" />
    <link rel="stylesheet" href="/css/custom.css" />
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/flowbite@1.5.3/dist/flowbite.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.0/css/all.min.css" />
    <script src="https://cdn.jsdelivr.net/npm/alpinejs@3.10.3/dist/cdn.min.js" defer></script>
    <script src="https://cdn.jsdelivr.net/npm/@yaireo/tagify"></script>
    <link rel="stylesheet" href="/css/animations.css" />
</head>
<body class="flex flex-col min-h-full bg-gray-100">
    <%- include('partials/header') %>
    
    <main id="page-content" class="flex-grow pt-24 pb-16 page-transition-enter">
        <div class="container px-4 mx-auto max-w-7xl sm:px-6 lg:px-8">
            <div class="mb-6">
                <a href="/tutors" id="back-to-tutors" class="text-indigo-600 hover:text-indigo-800">
                    <i class="mr-2 fas fa-arrow-left"></i>Back to Tutors
                </a>
            </div>
            
            <div class="grid grid-cols-1 gap-8 lg:grid-cols-3">
                <!-- Tutor Media and Basic Info -->
                <div class="lg:col-span-1">
                    <div class="overflow-hidden bg-white rounded-lg shadow-md">
                        <div class="p-6">
                            <div class="relative mb-6 overflow-hidden rounded-lg aspect-w-1 aspect-h-1">
                                <img src="<%= tutor.profile_photo || '/images/default-avatar.png' %>" alt="<%= tutor.username %>" class="object-cover w-full h-full">
                            </div>
                            <h1 class="mb-2 text-3xl font-bold text-gray-900"><%= tutor.username %></h1>
                            <p class="mb-4 text-lg text-gray-600"><%= tutor.email %></p>
                            <div class="flex items-center mb-4">
                                <span class="text-2xl font-bold text-green-600">$<%= tutor.hourly_rate %></span>
                                <span class="ml-2 text-gray-600">/hour</span>
                            </div>
                            <div class="mb-6">
                                <h2 class="mb-2 text-xl font-semibold text-gray-900">Subjects</h2>
                                <div class="flex flex-wrap gap-2">
                                    <% tutor.subjects.split(',').forEach(subject => { %>
                                        <span class="px-3 py-1 text-sm font-medium text-indigo-800 bg-indigo-100 rounded-full"><%= subject.trim() %></span>
                                    <% }) %>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                    <!-- Messages Card -->
                    <div class="mt-8 overflow-hidden bg-white rounded-lg shadow-md">
                        <div class="p-6">
                            <h2 class="mb-4 text-2xl font-semibold text-gray-900">Messages</h2>
                            <% if (user && user.id !== tutor.id) { %>
                                <form id="messageForm" class="space-y-4">
                                    <input type="hidden" name="recipientId" value="<%= tutor.id %>">
                                    <div>
                                        <label for="message" class="block mb-2 text-sm font-medium text-gray-700">Message</label>
                                        <textarea id="message" name="message" rows="4" class="block w-full px-3 py-2 mt-1 text-gray-700 border border-gray-300 rounded-md shadow-sm focus:border-indigo-500 focus:ring focus:ring-indigo-200 focus:ring-opacity-50" required></textarea>
                                    </div>
                                    <button type="submit" class="w-full px-6 py-3 text-white transition duration-300 bg-indigo-600 rounded-md hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2">
                                        Send Message
                                    </button>
                                </form>
                            <% } else if (!user) { %>
                                <p class="text-gray-700">Please <a href="/login" class="text-indigo-600 hover:text-indigo-800">log in</a> to contact this tutor.</p>
                            <% } else { %>
                                <p class="text-gray-700">This is your profile. You cannot send a message to yourself.</p>
                            <% } %>
                        </div>
                    </div>
                </div>

                <!-- Tutor Details -->
                <div class="lg:col-span-2">
                    <div class="overflow-hidden bg-white rounded-lg shadow-md">
                        <div class="p-6">
                            <div class="mb-6">
                                <h2 class="mb-2 text-2xl font-semibold text-gray-900">About Me</h2>
                                <p class="text-gray-700"><%= tutor.bio %></p>
                            </div>
                            
                            <div class="mb-6">
                                <h2 class="mb-2 text-2xl font-semibold text-gray-900">Availability</h2>
                                <p class="text-gray-700"><%= tutor.availability %></p>
                            </div>

                            <% if (tutor.profile_video) { %>
                            <div class="mb-6">
                                <h2 class="mb-2 text-2xl font-semibold text-gray-900">Introduction Video</h2>
                                <video src="<%= tutor.profile_video %>" controls class="w-full rounded-lg">
                                    Your browser does not support the video tag.
                                </video>
                            </div>
                            <% } %>

                            <div class="mb-6">
                                <h2 class="mb-2 text-2xl font-semibold text-gray-900">Reviews</h2>
                                <!-- Add review component here -->
                                <p class="text-gray-700">No reviews yet.</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <%- include('partials/footer') %>

    <script>
    document.getElementById('messageForm')?.addEventListener('submit', async (e) => {
        e.preventDefault();
        const form = e.target;
        const formData = new FormData(form);

        try {
            const response = await fetch('/messages/send', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify(Object.fromEntries(formData)),
            });

            if (response.redirected) {
                window.location.href = response.url;
            } else {
                const error = await response.json();
                alert(`Error: ${error.message}`);
            }
        } catch (error) {
            console.error('Error sending message:', error);
            alert('An error occurred while sending the message. Please try again.');
        }
    });

    // Add this new script for handling entrance animation
    document.addEventListener('DOMContentLoaded', () => {
        const pageContent = document.getElementById('page-content');
        pageContent.classList.add('page-transition-enter-active');
        pageContent.addEventListener('transitionend', () => {
            pageContent.classList.remove('page-transition-enter', 'page-transition-enter-active');
        });
    });

    // Add this script at the end of the file, before </body>
    document.getElementById('back-to-tutors').addEventListener('click', async (e) => {
        e.preventDefault();
        const url = e.target.closest('a').getAttribute('href');
        
        // Start exit animation
        document.getElementById('page-content').classList.add('page-transition-exit-active');
        
        // Wait for exit animation to complete
        await new Promise(resolve => setTimeout(resolve, 300));
        
        // Navigate to the tutors list page
        window.location.href = url;
    });
    </script>

    <%- include('partials/scripts') %>
</body>
</html>
